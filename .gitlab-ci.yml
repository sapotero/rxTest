# This file is a template, and might need editing before it works on your project.
# Read more about this script on this blog post https://about.gitlab.com/2016/11/30/setting-up-gitlab-ci-for-android-projects/, by Greyson Parrelli
image: openjdk:8-jdk

variables:
  ANDROID_COMPILE_SDK: "25"
  ANDROID_BUILD_TOOLS: "25.0.2"
  ANDROID_SDK_TOOLS: "25.2.0"

before_script:
  #- apt-get --quiet update --yes
  #- apt-get --quiet install --yes wget tar unzip lib32stdc++6 lib32z1
  #- wget --quiet --output-document=android-sdk.tgz https://dl.google.com/android/android-sdk_r${ANDROID_SDK_TOOLS}-linux.tgz
  #- tar --extract --gzip --file=android-sdk.tgz
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter android-${ANDROID_COMPILE_SDK}
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter platform-tools
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter build-tools-${ANDROID_BUILD_TOOLS}
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter extra-android-m2repository
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter extra-google-google_play_services
#  - echo y | android-sdk-linux/tools/android --silent update sdk --no-ui --all --filter extra-google-m2repository
  - export ANDROID_EMULATOR_FORCE_32BIT=true
  - export ANDROID_HOME=/home/sapotero/android-sdk/
  - export PATH=${PATH}:/home/sapotero/android-sdk/platform-tools:/home/sapotero/android-sdk/tools
  - chmod +x ./gradlew

stages:
  - build
  - test
  - copy

build:
  stage: build
  artifacts:
    untracked: true
  script:
    - ./gradlew assembleDebug
  artifacts:
    paths:
    - app/build/outputs/

unitTests:
  stage: test
  dependencies:
    - build
  script:
    - ./gradlew test

functionalTests:
  stage: test
  dependencies:
    - build
  script:
#    - wget --quiet --output-document=android-wait-for-emulator https://raw.githubusercontent.com/travis-ci/travis-cookbooks/0f497eb71291b52a703143c5cd63a217c8766dc9/community-cookbooks/android-sdk/files/default/android-wait-for-emulator
#    - chmod +x android-wait-for-emulator
#    - echo y  | /home/sapotero/android-sdk/tools/android --silent update sdk --no-ui --all --filter sys-img-x86-google_apis-${ANDROID_COMPILE_SDK}
#    - echo no | /home/sapotero/android-sdk/tools/android create avd -n test -t android-${ANDROID_COMPILE_SDK} --abi google_apis/x86
#    - /home/sapotero/android-sdk/tools/emulator64-x86 -avd test -no-window -no-audio &
#    - ./android-wait-for-emulator
#    - adb shell input keyevent 82
#    - ./gradlew cAT
    - echo 'empty test'

copy:
  stage: copy
  dependencies:
    - build
  script:
    #- curl -k -H "Authorization: Token 36b75fa39f3c0aac17080f2f05c8721f4a5c2d30" -F file=@test.txt -F filename=test.txt -F parent_dir=/ http://files.n-core.ru:8082/upload-api/024b6d67-3100-4c0e-b934-5b374673d23f
    - echo 'copy complited'